export const environment = {
  production: true,
  apiServiceHost: 'https://api.sportvvs.com' // This is the kubernetes service name for the spring boot api that serves the backend for this app
  // https://alligator.io/angular/environment-variables/
};
